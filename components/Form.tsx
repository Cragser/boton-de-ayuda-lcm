import * as React from 'react';
import {View, Text } from 'react-native';
interface FormProps {

}

const Form  = (props: FormProps) => {
 return (
  <View>
    <Text> Form </Text>
  </View>
 );
};

export default Form;
